{
  "name": "campussphere-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "set \"PATH=%PATH%;%ProgramFiles%\\nodejs\" && node scripts/dev-server.mjs",
    "build": "set \"PATH=%PATH%;%ProgramFiles%\\nodejs\" && node scripts/build.mjs",
    "preview": "set \"PATH=%PATH%;%ProgramFiles%\\nodejs\" && node scripts/dev-server.mjs --preview"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "esbuild-wasm": "^0.28.1"
  }
}
